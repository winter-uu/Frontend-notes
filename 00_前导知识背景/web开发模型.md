![[6348f74dd1b43b1f92c41b0a407664cd.png]]
#### 客户端（浏览器）的作用：
1、采集用户信息（在搜索框里搜索java漫画）
发起请求：将采集的信息携带发送给服务器（计算机）
2、展示信息（展示java漫画的商品）
拿到服务器给的结果后做一个处理来展示出来
#### 服务器（nb的计算机）
1、由java语言后台程序编写，拿到漫画java的信息去请求后台数据库
2、拿到数据库的结果后做封装返回给客户端
#### 数据库
例如mysql，查询漫画java，把结果返回给服务器
